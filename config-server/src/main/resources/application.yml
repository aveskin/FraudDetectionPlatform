server:
  port: 8888

spring:
  application:
    name: config-server
  profiles:
    active: native
  cloud:
    config:
      server:
        native:
          search-locations: file:///C:/Users/root/IdeaProjects/FraudDetectionPlatform/config-server/configs

# Actuator endpoints
management:
  endpoints:
    web:
      exposure:
        include: "*"